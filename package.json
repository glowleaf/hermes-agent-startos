{
  "name": "hermes-agent-startos",
  "version": "0.1.1",
  "description": "Hermes Agent packaged for StartOS — WebUI for LLM chat with multi-provider support",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run build:ts",
    "build:ts": "ncc build startos/index.ts -o dist",
    "build:s9pk": "start-sdk pack ."
  },
  "dependencies": {
    "@start9labs/start-sdk": "^1.4.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.3"
  }
}
